# Override « prod-like » — à combiner avec docker-compose-root.yml :
#   docker compose -f docker-compose-root.yml -f docker-compose.prod.yml up -d --build backend nginx
# (ou `make prod-up`).
#
# Différences avec le dev :
#   - backend servi par gunicorn (plus runserver)
#   - DEBUG=False, DJANGO_ENV=production, ALLOWED_HOSTS restreint
#   - nginx termine le TLS (cert auto-signé) devant le backend
services:

  backend:
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3
    environment:
      - DJANGO_ENV=production
      - DEBUG=False
      - ALLOWED_HOSTS=localhost,127.0.0.1,backend,nginx

  nginx:
    image: nginx:alpine
    container_name: rr_nginx
    depends_on:
      - backend
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./rr-infra/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./rr-infra/nginx/certs:/etc/nginx/certs:ro
    networks:
      - rr_network
